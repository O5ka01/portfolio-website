import BlogPage from '@/app/blog/page';

export default function EnglishBlogPage() {
  // This component serves as the English version of the blog page
  // It uses the same BlogPage component but with English context
  return <BlogPage />;
}
