
import Home from '@/app/page';

export default function EnglishHome() {
  // This component serves as the English version of the home page
  // It uses the same Home component but with English context
  return <Home />;
}
